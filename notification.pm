# package Notification;

# use Time::Piece;
# use Moose;

# has 'network' => ( is => 'ro' );
# has 'channel' => ( is => 'ro' );
# has 'nickname' => ( is => 'ro' );
# has 'received_at' => ( 
#   is => 'ro',
#   default => localtime,
# );

# no Moose;
# __PACKAGE__->meta->make_immutable;
